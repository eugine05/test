import React, { FC } from 'react';
import './App.css';
import Page from './components/Page';

/**
 * Компонент приложения
 * @returns {JSX.Element}
 * @constructor
 */
const App: FC = () => {
  return (
    <Page />
  );
};

export default App;
